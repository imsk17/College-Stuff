#include <stdio.h>

int main()
{
    int num1, num2, diff;
    printf("ENTER TWO INTEGERS: ");
    scanf("%d %d", &num1, &num2);
    diff = num1 - num2;
    printf("\nDifference is: %d\n", diff);
    return 0;
}
